code/await Snds (void)
                    => (var& _Mix_Chunk_ptr snd_start)
                        => void
do
    spawn SDL_open_sound("snds/start.wav") => (start);
    snd_start = &start;
    await FOREVER;
end
