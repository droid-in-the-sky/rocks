code/await Snds (void)
                    => (var& _Mix_Chunk_ptr snd_start)
                        => void
do
    watching SDL_open_sound("snds/start.wav") => (start) do
        snd_start = &start;
        await FOREVER;
    end
end
